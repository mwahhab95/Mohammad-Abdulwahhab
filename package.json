{
  "name": "mohammad-abdulwahhab-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "generate": "py -3 scripts/generate_site.py",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.1.0"
  }
}
